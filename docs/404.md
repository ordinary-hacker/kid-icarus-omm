---
layout: default
title: "404"
permalink: /404.html
nav_exclude: true
search_exclude: true
---

# Oops

Whatever you were trying to access doesn't exist, no longer does, or you / we are having technical difficulties.

You may want to return to the [homepage]({{ '/' | relative_url }}).

<img id="eggplant" src="{{ '/assets/images/eggplant1.svg' | relative_url }}" width="512" height="512">

<script>
faviconIdle = `${faviconBase}favicon_404_idle.svg`;

faviconAnimFrames = [
    `${faviconBase}favicon_404_anim1.svg`,
    `${faviconBase}favicon_404_anim2.svg`
];

const eggplantAnimFrames = [
    `${imagesBase}eggplant1.svg`,
    `${imagesBase}eggplant2.svg`,
    `${imagesBase}eggplant3.svg`
];

const eggplant = document.getElementById("eggplant");

let eggplantFrame = 0;

setInterval(() => {
    eggplantFrame = (eggplantFrame + 1) % eggplantAnimFrames.length;
    eggplant.src = eggplantAnimFrames[eggplantFrame];
}, 170);
</script>
